# Agent Orchestration

> This file is regenerated by the installer based on which agents are
> actually installed. Run `install.py --refresh-agents` to rebuild it.

## Available Agents

(populated at install time)

## Immediate Agent Usage

Invoke these agents proactively — no user prompt needed.

## Parallel Task Execution

Dispatch agents in parallel when their work is independent.
Multiple Agent tool uses in one assistant message.

## Multi-Perspective Analysis

For complex problems, use split-role subagents in parallel.
